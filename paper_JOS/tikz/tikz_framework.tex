\begin{tikzpicture}[
    node distance=1.4cm and 1.6cm,
    box/.style={draw, rectangle, rounded corners, minimum width=2.5cm, minimum height=0.9cm, align=center, fill=blue!10, font=\small},
    snap/.style={draw, rectangle, rounded corners, minimum width=1.6cm, minimum height=0.7cm, align=center, fill=blue!5, font=\footnotesize},
    pareto/.style={draw, ellipse, minimum width=2.2cm, minimum height=0.8cm, align=center, fill=green!10, font=\small},
    stability/.style={draw, rectangle, minimum width=2.4cm, minimum height=0.9cm, align=center, fill=orange!15, font=\small},
    arrow/.style={-latex, thick}
]

% Top row: nominal instance unfolded into reactive snapshots
\node[box] (inst) {Nominal\\instance $s_0$};
\node[snap, right=of inst] (s1) {$s_1$};
\node[snap, right=0.5cm of s1] (s2) {$s_2$};
\node[snap, right=0.5cm of s2] (sdots) {$\cdots$};
\node[snap, right=0.5cm of sdots] (sT) {$s_T$};

\draw[arrow] (inst) -- (s1) node[midway, above, font=\scriptsize] {perturb.};
\draw[arrow, dashed] (s1) -- (s2) node[midway, above, font=\scriptsize] {perturb.};
\draw[arrow, dashed] (s2) -- (sdots);
\draw[arrow, dashed] (sdots) -- (sT);

% Middle row: per-snapshot ε-constraint CP solve with the selector under test
\node[box, below=1.3cm of s2] (cp) {$\varepsilon$-constraint CP\\ + conflict-weighting\\ selector under test};
\draw[arrow] (inst) -- (cp);
\draw[arrow] (s1) -- (cp);
\draw[arrow] (s2) -- (cp);
\draw[arrow] (sdots) -- (cp);
\draw[arrow] (sT) -- (cp);

% Right: two-objective Pareto front per snapshot
\node[pareto, right=1.2cm of cp] (pareto) {Pareto front\\$(C_{max}, F)$};
\draw[arrow] (cp) -- (pareto);

% Bottom: representative solution → stability and flexibility diagnostics
\node[stability, below=1.1cm of pareto] (stab) {Stability \&\\flexibility diagnostics};
\draw[arrow] (pareto) -- node[right, font=\scriptsize] {rep. solution} (stab);

\end{tikzpicture}
